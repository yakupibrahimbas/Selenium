package Subat18;

import day08_relativeLocators_dropdown_testbase.Utilities.TestBase;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.openqa.selenium.By;

public class C1 extends TestBase {
 /*@Test
    public void test01() {
        driver.get("http://automationexercise.com%22/);
        String actualTitle = driver.getTitle();
        String expectedTitle = "Automation Exercise";
        Assertions.assertTrue(actualTitle.contains(expectedTitle));
        Byxpath("//a[@href='/login']").click();

        String actualText = Byxpath("//h2[contains(text(), "New User Signup!")]").getText();
        String expectedText = "New User Signup!";
        Assertions.assertTrue(actualText.contains(expectedText));

        Byxpath("//input[@type='text']").sendKeys("Necati");
        Byxpath("//input[@data-qa='signup-email']").sendKeys("ylok@gmail.com");
        Byxpath("//button[@data-qa='signup-button']").click();

        WebElement inCorrect = Byxpath("//p[.='Email Address already exist!']");
        boolean displayedinCorrect =inCorrect.isDisplayed();
        Assertions.assertTrue(displayedinCorrect);
        String actualinCorrectText =inCorrect.getText();
        String expectedinCorrectText ="Email Address already exist!";
        Assertions.assertTrue(actualinCorrectText.contains(expectedinCorrectText)); */
}
